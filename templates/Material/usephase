const workflow = new PhaseManager();

console.log(workflow.get()); // "init-upl"

// Move forward step-by-step
workflow.next(); 
console.log(workflow.get()); // "01ReadSpreadsheet"

// Skip phases due to a conditional branch
if (someCondition) {
  workflow.skipTo(Phase.WANT_RESULTS);
}

// Error handling / Failure branch jump
try {
  // complex code
} catch (err) {
  workflow.skipTo(Phase.CLEANUP_FAILURE);
}

// Check timeline progress
if (workflow.hasPassed(Phase.IDENTIFY_EXIST)) {
  console.log("Database lookup is already complete.");
}
